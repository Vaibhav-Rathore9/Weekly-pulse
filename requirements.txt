# Phase 1: Foundation & Ingestion
requests>=2.31.0
google-play-scraper>=1.2.7
pandas>=2.1.0
click>=8.1.0

# Phase 2: Processing Pipeline (install when needed)
# umap-learn>=0.5.5
# hdbscan>=0.8.33
# litellm>=1.0.0
# numpy>=1.24.0
# scikit-learn>=1.3.0

# Phase 3: Rendering (install when needed)
# jinja2>=3.1.0

# Phase 4: Orchestration (install when needed)
# pyyaml>=6.0

# Phase 6: MCP Delivery (install when needed)
# mcp>=1.0.0
